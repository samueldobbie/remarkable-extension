import { createTheme } from "@mui/material"

const theme = createTheme({
  palette: {
    primary: {
      main: "rgb(93 123 255)",
    },
    secondary: {
      main: "#eceded",
    },
    text: {
      primary: "#333",
      secondary: "#eceded",
    }
  }
})

export { theme }
