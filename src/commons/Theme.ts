import { createTheme } from "@mui/material"

const theme = createTheme({
  palette: {
    primary: {
      main: "rgb(139 217 181)",
    },
    secondary: {
      main: "#eceded",
    },
    text: {
      primary: "#333",
      secondary: "#eceded",
    }
  }
})

export { theme }
